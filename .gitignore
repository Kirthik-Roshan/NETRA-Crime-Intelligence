node_modules/
.env
.next/
.next-dev/
dist/
build/
coverage/
*.log
*.db-shm
*.db-wal

# build output
/.next/
/out/
tsconfig.tsbuildinfo

# function config carries deploy secrets — never commit
functions/*/catalyst-config.json
functions/ai_quickml/catalyst-config.json

# local env & secrets (never commit) — .env.example stays tracked
.env
.env*.local

# local editor / agent config
.claude/

# generated KB text (regenerate from source; not source of truth)
/data/kb_txt_gen/

# Catalyst CLI local project binding (per-developer, not for the repo)
.catalystrc
functions/*/package-lock.json
